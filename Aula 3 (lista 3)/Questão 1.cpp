#include <stdio.h>

int main(){
	int num=0, m = 0;
	while (num >= 0){
	scanf("%d", &num);
	if m <= num{
		m = num;
	}
	printf("Voce digitou um numero negativo, por isso o programa parou, o maior numero digitao foi %d", m);
	}
}
