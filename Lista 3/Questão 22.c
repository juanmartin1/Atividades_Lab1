#include <stdio.h>

int main(){
	int i, med, not;
	float med
	for(i=0 ; i<15 ; i++){
		scanf("%d", &nota);	
	}
	
	
	for(i=0 ; i<10 ; i++){
		if (V[i] < 0){
			V[i] = 0;
		}
	}
	
	for(i=0 ; i<10 ; i++){
		printf("%d", V[i]);
	}
}
